services:
  gemini:
    image: node:20
    container_name: gemini
    working_dir: /workspace
    volumes:
      - ./:/workspace
      - gemini:/root/.npm
    stdin_open: true
    tty: true
    command: bash

volumes:
  gemini: